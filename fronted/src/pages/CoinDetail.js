import React from "react";
import { useParams } from "react-router-dom";

const CoinDetail = ({ coin }) => {
  // const parameter = useParams();
  // const myCoin = coin.filter((c) => c.id === parameter);
  return console.log(coin);
};

export default CoinDetail;
